extends layout

block content
  h1= title
  p Welcome to #{title}

  form(method="post", enctype="multipart/form-data", action="/photos")
    input#files(type="file", name="file")
    input(type="submit")

